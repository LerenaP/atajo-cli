#!/usr/bin/env node

/*
//INSTALL PM2
console.log("Installing PM2 Process Manager (Please wait. It can take a while)");
let exec = require('child_process').exec;
let child = exec('npm install -g pm2',
    (error, stdout, stderr) => {

        if (error) { console.error(error); }
        if (stdout) { console.error(stdout); }
        if (stderr) { console.error(stderr); }

    });


//INSTALL CODE-PUSH
console.log("Installing Code-Push (Please wait. It can take a while)");
exec = require('child_process').exec;
child = exec('npm install -g code-push',
    (error, stdout, stderr) => {

        if (error) { console.error(error); }
        if (stdout) { console.error(stdout); }
        if (stderr) { console.error(stderr); }

    });
*/